export interface ExceptionType {
    id: string
    orgId: string
    riderId: string
    date: Date
    pickupStopId: string
    guardianId: string
    dropOffStopId: string
    createdBy: string
    createdDate: Date
    lastEditedBy: string
    lastEditDate: Date
}