export interface StopType {
    address: string
    id: string
    orgId: string
    routeId: string
    riderIds?: string[]
    stopName: string
    createdBy: string
    createdDate: Date
    lastEditedBy: string
    lastEditDate: Date
}
