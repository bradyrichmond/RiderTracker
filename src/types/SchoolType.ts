export interface SchoolType {
    id: string
    schoolName: string
    orgId: string
    address: string
    riderIds?: string[]
    createdBy: string
    createdDate: number
    lastEditedBy: string
    lastEditDate: number
}
