import Grid from "@mui/material/Unstable_Grid2"

const ActiveRoute = () => {
    return (
        <Grid container spacing={2}>
            <Grid xs={12}>
                Need to figure out how to store/recover active route for driver
            </Grid>
        </Grid>
    )
}

export default ActiveRoute